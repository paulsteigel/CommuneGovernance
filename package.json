{
  "name": "communegovernance",
  "version": "1.0.0",
  "description": "CARE Vietnam — Rural Data Collection API (Cloud Functions + Firestore)",
  "main": "index.js",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.2.5",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}
